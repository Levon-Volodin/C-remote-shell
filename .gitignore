# -- Build artifacts --
*.exe
*.obj
*.pdb
*.ilk

# -- Compiled object / binary blobs (auto-generated by make blob) --
client/inject/loader.o
client/inject/loader_func.bin

# -- Auto-generated build config (contains embedded key -- never commit) --
megaploit_build_config.h

# -- Cryptographic key - never commit --
secret.key

# -- Windows shell artefact (2>NUL in Makefile creates a real file on Windows) --
NUL

# -- Local override Makefile (developer-specific, not for the repo) --
Makefile.local
