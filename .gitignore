# -- Build artifacts --
*.exe
*.obj
*.pdb
*.ilk

# -- Compiled object / binary blobs (auto-generated by make) --
client/loader.o
client/loader_func.bin
client/loader_blob.h

# -- Cryptographic key - never commit --
secret.key

# -- Windows shell artefact (2>NUL in Makefile creates a real file on Windows) --
NUL
